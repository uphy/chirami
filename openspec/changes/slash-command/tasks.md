## 1. コマンドピッカー基盤の実装

- [x] 1.1 `src/extensions/slashCommand.ts` を新規作成し、コマンドの型定義（`SlashCommand` interface: `id`, `label`, `description`, `execute(view)`）を定義する
- [x] 1.2 `ViewPlugin.update()` で `docChanged` を監視し、現在行の行頭から `/` + 英字が続くパターンを検出してピッカーの開閉を制御する（パススルー方式：文字は常にエディタに挿入される）
- [x] 1.3 ピッカー DOM（`ul` リスト）を構築する関数を実装する（コマンド名・説明を表示）
- [x] 1.4 `document.body` への append / remove でピッカーの開閉を管理する関数を実装する
- [x] 1.5 カーソル位置の `getBoundingClientRect()` を使い `position: fixed` でピッカーを配置する。画面下端にはみ出す場合は上方向に表示する

## 2. キーボード操作の実装

- [x] 2.1 ↑↓・Enter・Escape のキーハンドラを実装する（文字入力は EditorView に渡す。ピッカーは `update()` でエディタ状態を読んでフィルタを更新する）
- [x] 2.2 ↑↓ キーでハイライトアイテムを移動する処理を実装する
- [x] 2.3 Enter キーで選択中コマンドを実行し、現在行の `/xxx` をブロックテキストで置換してピッカーを閉じる処理を実装する
- [x] 2.4 Escape キーでピッカーをキャンセルする処理を実装する（エディタには `/xxx` がそのまま残る）
- [x] 2.5 フィルタ結果がゼロになったときにピッカーを自動的に閉じる処理を実装する

## 3. コマンドの実装

- [x] 3.1 `/tldraw` コマンド: ` ```tldraw\n\n``` ` を挿入し、直後に `openTldrawOverlay` を呼び出す
- [x] 3.2 `/mermaid` コマンド: ` ```mermaid\n\n``` ` を挿入し、カーソルをブロック内の空行に配置する
- [x] 3.3 `/table` コマンド: `| | |\n|---|---|\n| | |` テンプレートを挿入し、カーソルを1行目の最初のセル（`| ` の後）に配置する

## 4. スタイリング

- [x] 4.1 コマンドピッカーのスタイル（`.cm-slash-picker`、アイテム、ハイライト状態）を `src/style.css` に追加する

## 5. エディタへの組み込み

- [x] 5.1 `slashCommandExtension` を `src/editor.ts` に追加する

## 6. 動作確認

- [ ] 6.1 行頭 `/` でピッカーが表示され、行の途中では表示されないことを確認する
- [ ] 6.2 文字入力でフィルタリングされ、Enter で挿入されることを確認する
- [ ] 6.3 Escape でキャンセルされ `/` が残ることを確認する
- [ ] 6.4 `/tldraw` → オーバーレイが即座に開くことを確認する（手動確認）
- [ ] 6.5 `/mermaid` → ブロック挿入後カーソルがブロック内にあることを確認する（手動確認）
- [ ] 6.6 `/table` → テンプレートが挿入され、カーソルが先頭セルにあることを確認する（手動確認）
