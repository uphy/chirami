# Ideas

未実装のアイデア置き場。実装が決まったものは通常の開発フローで進め、実装後はこのディレクトリから削除して `docs/` の該当ページへ反映する。

ここにあるのは「やると決まった仕様」ではなく「検討済みの設計メモ」であり、実装時に見直してよい。

| アイデア | 概要 |
|---------|------|
| [chirami-append](chirami-append.md) | CLI から note ID を指定してノートへ追記する |
| [cli-feedback](cli-feedback.md) | `chirami confirm` / `input` / `select` で CLI にユーザーの応答を返す |

> 2026-07-26 に openspec ワークフローを廃止した際、`openspec/changes/` にあった未実装提案をここへ移した。廃止前の全履歴（アーカイブ済み提案・spec）は git 履歴（`a9e1318` 時点の `openspec/`）に残っている。
